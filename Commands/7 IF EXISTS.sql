CREATE DATABASE IF NOT EXISTS test_file 

/* IF NOT EXISTS can be used to check whether we already have that database or not and 
if not then we'll create that databse, on running this command we wont get any error, if 
the database exists we'll get a warning 
*/

/* Similary We can also use it while dropping databse
DROP DATABASE IF EXISTS database_name
Here we are dropping the database if it exists
*/

